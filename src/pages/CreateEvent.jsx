export default function CreateEvent() {
  return (
    <div className="max-w-lg space-y-3">
      <h1 className="text-2xl font-semibold">Create Event</h1>
      {/* replace with real form later */}
      <p>Only visible when authenticated (Protected route).</p>
    </div>
  );
}
